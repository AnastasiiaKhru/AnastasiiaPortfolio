[build]
builder = "nixpacks"
buildCommand = "dotnet publish AnastasiiaPortfolio.csproj -c Release -o out"

[deploy]
startCommand = "dotnet out/AnastasiiaPortfolio.dll"
healthcheckPath = "/"
healthcheckTimeout = 100

[env]
ASPNETCORE_ENVIRONMENT = "Production"
ASPNETCORE_URLS = "http://+:5000" 